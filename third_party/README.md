# Third-party dependencies

Dependencies are fetched into this directory by pinned manifests/scripts and
are ignored by Git. Each integration must record its exact commit, upstream URL,
license, local patches, and checksums. No dependency may silently track a branch.

